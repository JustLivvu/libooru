# Nginx config snippet for Libooru
# Place inside a server {} block

# location / {
#     root /usr/local/www/libooru;
#     index index.php;
#     try_files $uri $uri/ /index.php$is_args$args;
# }
#
# # Block sensitive PHP files (not index.php)
# location ~* ^/(config|db|auth|post|view|image|api)\.php$ {
#     return 403;
# }
#
# # Block data directory
# location /data/ {
#     return 403;
# }
#
# # PHP-FPM
# location ~ \.php$ {
#     fastcgi_pass unix:/var/run/php-fpm.sock;  # adjust as needed
#     fastcgi_index index.php;
#     include fastcgi_params;
#     fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
#     fastcgi_param PATH_INFO $fastcgi_path_info;
#     client_max_body_size 32m;
# }
